interface AIBlurOverlayProps {
  text: string;
  visibleSentences?: number;
}

// SolarMatch is fully free — AI content is never blurred.
export default function AIBlurOverlay(_: AIBlurOverlayProps) {
  return null;
}
